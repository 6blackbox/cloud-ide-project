"use client";import { PanelResizeHandle } from "react-resizable-panels";export default function ResizeHandle({ className = "", id }) {  return (    <PanelResizeHandle      className={`group w-2 flex justify-center items-center cursor-col-resize bg-transparent transition-colors hover:bg-[var(--accent)]/10 focus:outline-none ${className}`}      id={id}    >      {}      <div className="w-[1px] h-full bg-[var(--border-color)] group-hover:bg-[var(--accent)] transition-colors" />    </PanelResizeHandle>  );}